<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">#323032</item>
        <item name="android:colorBackground">@color/cinza_medio</item>
    </style>

    <!-- Tema para tela de login -->
    <style name="AppThemeLogin" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">#3CAC88</item>
        <item name="colorAccent">@color/azulEscuro</item>
        <item name="android:colorBackground">@color/cinza_medio</item>
    </style>

</resources>
